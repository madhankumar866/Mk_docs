To Login using public key without password's


ssh-keygen -t rsa

ssh root@target-ip mkdir -p .ssh

cat .ssh/id_rsa.pub | ssh root@target-ip 'cat >> .ssh/authorized_keys'
ssh root@target-ip "chmod 700 .ssh; chmod 640 .ssh/authorized_keys"
ssh root@target-ip

{

Url-s

https://www.ssh.com/ssh/copy-id

}